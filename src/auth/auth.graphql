type Query {
  getRefreshToken(accessToken: String!): String!
}

type Token {
  accessToken: String!
  refreshToken: String!
}

type RefreshToken {
  userId: ID!
  refreshToken: String!
}

input RefreshTokenInput {
  userId: ID!
  refreshToken: String!
}
