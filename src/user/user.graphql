type Query {
  getUsers: [User]
  getUser(id: String!): User
}

type Mutation {
  createUser(createUser: CreateUserInput!): User
  updateUser(updateUser: UpdateUserInput!): User
  deleteUsers(deleteUser: [String!]!): [String]
  addRole(addRole: AddRoleInput!): User
}

type User {
  id: ID!
  name: String!
  lastName: String!
  siteNumber: Int!
  createdAt: Date
  updatedAt: Date
  role: [Role]
}

scalar Date

input CreateUserInput {
  name: String!
  lastName: String!
  siteNumber: Int!
  role: RoleType
}

input AddRoleInput {
  userId: ID!
  roleId: ID!
}

input UpdateUserInput {
  id: ID!
  name: String
  lastName: String
  siteNumber: Int
}

input DeleteUserInput {
  ids: [ID!]!
}
